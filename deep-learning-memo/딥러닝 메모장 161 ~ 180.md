# 161
```
%matplotlib inline
이 줄이 필요한 이유

matplotlib은 원래 독립된 창을 새로 띄워 그래프를 보여주는 시각화 라이브러리 입니다.
하지만 주피터 노트북은 웹 브라우저 안에서 동작한다.
%matplotlib inline은 주피터에게 "그래프를 팝업으로 띄우지 말고, 브라우저 셀 결과창에 보여줘"라고 지시하는 명령어이다.
```
# 162
```
with 구문을 쓰는 이유

[with 문을 쓰지 않았을 때]
img = Image.open(full_image_path)
img_width, img_height = img.size
# 파일이 여전히 열려있는 상태

ㅇ 위처럼 코드를 짜면 size 정보를 읽은 뒤에도 이미지 파일이 os 메모리상에 계속 열린 채로 남게 된다.
ㅇ 이 상태로 반복문을 돌면..
	Too many open files 에러: 운영체제가 한 프로세스당 동시에 열 수 있는 파일 개수 초과
	메모리 누수: 쓰지 않는 파일 자원이 닫히지 않고 계속 램에 쌓인다.
	파일 잠금 문제: 다른 프로세스가 해당 파일을 수정하거나 삭제하려 할때 "이미 다른 프로그램에서 사용 중입니다"하고 거부당한다.

따라서 with문을 쓰지 않으려면 아래처럼 .close()를 호출해줘야 한다.
img.close()

[with 문을 썼을 때]
with Image.open(full_image_path) as img:
	img_width, img_height = img.size
	# with 블록을 빠져나가는 순간, img.close()를 자동으로 호출해서 파일을 닫는다.
```
![](images/Pasted%20image%2020260724110533.png)
# 163
```
seaborn은 matplotlib을 기반으로 만든 파이썬 통계 데이터 시각화 라이브러리.
복잡한 그래프를 간단한 코드로 그려줍니다.

대표 기능
ㅇ 관계형 플롯: 변수 간의 관계를 보여주는 산점도나 선 그래프(scatterplot, lineplot)

import seaborn as sns
sns.lineplot(data=df, x='epoch', y=train_loss_col, 
             label='Train Loss', color='#141140', linestyle='-', linewidth=2)
sns.lineplot(data=df, x='epoch', y=val_loss_col, 
             label='Validation Loss', color='orangered', linestyle='-')
```
![](images/Pasted%20image%2020260729141022.png)
# 164
```
class_id, x, y, bw, bh = map(float, line.strip().split())

ㅇ line.strip(): 줄 끝에 붙어있는 눈에 안보이는 개행 문자(\n)나 공백을 싹 지워줍니다.
ㅇ .split(): 공백을 기준으로 텍스트를 하나씩 잘라서 리스트로 만듭니다.
	"0 0.45 0.62 0.20 0.15" 문자열이 ["0", "0.45", "0.62", "0.20", "0.15"] 리스트로
ㅇ map(float, ...): 잘라낸 글자들은 아직 문자열 형태입니다. 실수 연산을 할 수 있도록 float 변환
ㅇ 변환된 5개의 숫자를 각각의 변수에 언패킹합니다.
```
# 165
```
yaml_content = yaml.load(f, Loader=yaml.FullLoader)

Loader=yaml.FullLoader: yaml 파일을 읽을 때 보안 위험이 있는 임의의 코드 실행을 막고, 안전하게 파이썬 데이터 타입으로 매핑해주는 로더를 지정한 것임.
결과적으로 yaml_content 안에는 {'train': '', 'val': ''} 같은 딕셔너리가 들어감

print(yaml.dump(yaml_content, default_flow_style=False))
yaml.dump(...): 파이썬 딕셔너리로 바꿨던 yaml_content를 다시 이쁘게 정렬된 yaml 형태의 텍스트로 되돌려 출력합니다.

default_flow_style=False
이 옵션이 없거나 True라면 한줄로 지저분하게 표시됩니다.
False로 설정하면 우리가 보기 편하게 표시됩니다.
```
# 166
```
객체 탐지 학습을 돌리기 전, 이미지들의 사이즈를 체크하는 이유:
YOLO 모델에 이미지를 넣으려면 기본적으로 모든 이미지 텐서의 shape이 동일해야 하나의 배치로 묶어서 GPU 연산을 할 수 있습니다.

그렇다고 모든 이미지 사이즈가 640으로 같다면.. imgsz=640을 명시하지 않아도 될까?
"원칙적으로는 안 써도 동작하지만, 무조건 명시해 주는 것이 국룰이다."
다음에 다른 해상도로 실험해볼 때 빠르게 값만 바꿔서 테스트 가능하다.
```
# 167
```
fig, axes = plt.subplots(2, 4, figsize=(20, 11))

for ax, img_file in zip(axes.ravel(), selected_images):
...


zip(axes.ravel(), selected_images) 뜯어보기

ㅇ axes.ravel()이 하는 일(2차원 -> 1차원 펼치기)

axes = [
  [ax1, ax2, ax3, ax4], 
  [ax5, ax6, ax7, ax8]	
]
axes.ravel()또는 axes.flatten()을 쓰면 2차원을 1차원 리스트로 쫙 펼쳐줍니다.
[ax1, ax2, ax3, ax4, ax5, ax6, ax7, ax8]


ㅇ zip()이 하는 일
여러개의 리스트에서 같은 인덱스에 있는 요소끼리 1:1로 짝을 지어주는 함수입니다.
axes_list = [ax1, ax2, ax3, ax4, ax5, ax6, ax7, ax8] # 8개
selected_images = [img1, img2, img3, ..., img8, img9] # 9개

zip(axes_list, selected_images)를 돌리면
(ax1, img1)
(ax2, img2)
...
(ax8, img8)
그리고 찌끄래기 9번째 사진은 버려집니다.
```
# 168
```
파이썬의 random.sample() 함수는 시퀀스(리스트, 튜플, 문자열 등)나 집합에서 중복 없이 무작위 추출합니다.

import random
random.sample(sequence, k)

sequence: 샘플 추출할 대상
k: 추출할 표본 개수
```
# 169
```
seed=0
	난수 생성기를 0으로 고정한다. 언제 어디서든 코드를 실행시켜도 결과가 똑같이 재현되도록..

optimizer='auto'
	YOLOv8 내부 알고리즘이 모델 크기나 데이터셋 특성을 고려해 최적의 알고리즘(AdamW 또는 SGD)을 자동으로 선택하도록 맡기는 옵션

lrf=0.1 (Final Learning Rate Factor)
	학습이 끝날때 최종 학습률을 lr0 * lrf로 만들어 점차 줄여나가는 비율
	학습이 진행될수록 lr이 점점 감소.

dropout=0.1
	학습 진행중 신경망의 일부 노드를 10% 확률로 무작위로 꺼버리고 학습합니다.
	암기식 학습인 과적합을 막고, 모델이 데이터의 특징을 학습하도록 유도

```
![](images/Pasted%20image%2020260729200123.png)
# 170
```
str.strip()
문자열의 앞뒤에 붙은 불필요한 공백(스페이스, 탭, 줄바꿈)을 싹 지워주는 함수입니다.
```
![](images/Pasted%20image%2020260729210120.png)
# 171
```
random.choice() vs random.sample()

ㅇ random.choice()
	리스트에서 하나의 값만 무작위로 추출
	결과가 리스트가 아닌 단일 값 자체로 반환

ㅇ random.sample()
	리스트에서 지정한 개수 만큼 무작위로 추출
	중복을 허용하지 않습니다.(비복원 추출)
	결과는 항상 리스트 형식으로 반환

ㅇ random.choices()
	리스트에서 중복을 허용하여 여러 개를 무작위로 추출
[파이썬 코드]
import random

items = ['Apple', 'Banana', 'Cherry']

result = random.choices(items, k=5)
print(result) # ['Banana', 'Apple', 'Banana', 'Cherry', 'Apple']
```
# 172
```
Counter에 대해 알아보자.

from collections import Counter

Counter는 딕셔너리(dict)의 특수 확장판입니다.
딕셔너리는 존재하지 않는 키(key)에 접근해서 값을 더하려고 하면 KeyError가 터집니다.

딕셔너리라면 아래와 같이 코드가 복잡해집니다.
[파이썬 코드]
class_counts = {}
if cls not in class_counts:
	class_counts[cls] = 0 # 키가 없으면 0으로 초기화해주는 작업 필수
class_counts[cls] += 1

하지만 Counter는 존재하지 않는 키에 접근해도 에러를 뱉지 않고 기본값 0을 알아서 반환해줍니다.
[파이썬 코드]
class_counts = Counter()
...
class_counts[cls] += 1
```
# 173
```
sorted_counts = dict(sorted(named_counts.items(), key=lambda x: x[1], reverse=True))

key=lambda x: x[1]
	튜플의 0번째(x[0], 이름)가 아니라 1번째(x[1], 개수)를 기준으로 정렬하라는 의미

reverse=True
	내림차순으로 정렬
```
![](images/Pasted%20image%2020260730135231.png)
# 174
```
fig, axes = plt.subplots(1, 3, ...)
ㅇ 1행 3열의 그래프 도화지(axes)를 만듭니다.
axes[0], axes[1], axes[2]가 각각 3개의 그래프 위치를 담당합니다.
```
![](images/Pasted%20image%2020260730233421.png)
# 175
```
img = cv2.imdecode(np.fromfile(cm_path, dtype=np.uint8), cv2.IMREAD_COLOR)

보통 이미지를 읽을 때 우리는 아래처럼 간단하게 씁니다.
img = cv2.imread(cm_path)

하지만 cv2.imread()는 C++로 작성되어 있어서 파일경로에 한글이나 특수문자가 있으면 이미지를 못 읽고 None을 반환합니다.
그래서 이 에러를 방지하기 위해 "파일 경로를 거치지 않고, 파일 데이터를 바이너리(숫자)로 읽어서 이미지로 복원"하는 것입니다.

[1단계] np.fromfile(cm_path, dtype=np.uint8)
파일 경로(cm_path)에 있는 이미지 파일을 한글 경로 영향 없이 무지성 1차원 데이터(바이트 배열)로 읽어옵니다.
dtype=np.uint8
	컴퓨터에서 파일 바이트는 0~255 숫자(unsigned int 8bit)로 구성되어 있습니다.
	즉, 이미지 파일을 0~255 1차원 숫자 배열 형태로 넘파이에 로드합니다.

[2단계] cv2.imdecode(..., cv2.IMREAD_COLOR)
1단계에서 읽어온 1차원 숫자 배열을 진짜 3차원 컬러 이미지로 복원(decode)합니다.
cv2.IMREAD_COLOR
	읽어온 데이터를 컬러(BGR) 형태로 풀라는 옵션입니다.
```
![](images/Pasted%20image%2020260730235259.png)
# 176
```
val_metrics = best_model.val(split='val', workers=0)

split='val'
	ㅇ 의도: data.yaml 파일에 명시된 데이터 분할 중 어떤 데이터 세트로 시험을 볼 것인지 지정하는 파라미터
	ㅇ 들어갈 수 있는 값: 'train', 'val', 'test'

workers=0
	ㅇ 의도: 데이터를 읽어올 때 사용할 CPU 멀티프로세싱 데이터로더 스레드/프로세스 개수를 설정합니다.
	0으로 설정한 이유?
		숫자를 크게 잡으면 데이터를 병렬로 빨리 읽어올 수 있습니다.
		하지만 파이썬 멀티프로세싱 메모리 충돌로 Shared memory error나 CUDA out of memory, Dataloader deadlock같은 갑작스러운 에러가 터집니다.
		workers=0으로 하면 메인 프로세스(단일 스레드) 혼자 안전하게 데이터를 읽어오므로, 속도는 약간 느려도 에러가 거의 발생하지 않습니다.

pd.DataFrame.from_dict(...) & .round(3)

best_model.val()을 실행하면 그 안에는 results_dict가 있는데 이 안에는 많은 평가 수치들이 딕셔너리 형태로 담겨 있습니다.
# val_metrics.results_dict 예시 
{ 
	'metrics/precision(B)': 0.854321, 
	'metrics/recall(B)': 0.812345, 
	'metrics/mAP50(B)': 0.889123, 
	... 
}

pd.DataFrame.from_dict(..., orient='index', columns=['Metric Value'])
	의도: 파이썬 딕셔너리를 판다스 DataFrame 형태로 이쁘게 변환합니다.
	orient='index'
		딕셔너리의 key값을 표의 행 이름(index)으로 세로로 배치하라는 뜻입니다.
	columns=['Metric Value']
		데이터가 들어갈 열(Column)의 이름을 지정합니다.
```
![](images/Pasted%20image%2020260731001953.png)
![](images/Pasted%20image%2020260731001906.png)
# 177
```
latest_weight_path = max(weight_files, key=os.path.getctime)

os.path.getctime(path)
	ctime은 Creation Time의 약자입니다.
	특정 파일 경로를 넣으면 그 파일이 생성된 시각을 숫자 형태로 반환합니다. 최근에 만들어진 파일일수록 숫자가 더 큽니다.

key=os.path.getctime
	파이썬의 max() 함수는 기본적으로 알파벳 순으로 가장 큰 값을 찾습니다.
	하지만 key 옵션에 os.path.getctime을 넣어주면, 알파벳으로 비교하지 말고, 생성시간을 기준으로 비교해라는 뜻입니다.
```
![](images/Pasted%20image%2020260731112007.png)
# 178
```

[준비 단계]
비디오를 한 프레임씩 읽을 카메라(cap)와, 결과를 다시 저장할 비디오 녹화기(out_writer) 준비

[영상 가공 및 AI 탐지 (반복문)]
ㅇ 관심 없는 위/아래 영역을 검은색으로 지운다 (ROI 마스킹)
ㅇ YOLO 모델로 차량을 찾는다
ㅇ 지웠던 위/아래 배경을 다시 원래 영상으로 복원한다
ㅇ 좌우 차선 영역에 사다리꼴 다각형(ROI)를 그리고, 차 중심점 기준으로 좌우 차량 수를 센다
ㅇ 화면 우측 상단에 정체 여부(heavy smooth) 텍스트를 적어 넣는다.

[마무리 단계]
비디오 파일 저장을 완료하고 자원을 해제(release)한다

[1] 검은색으로 지웠다 복원하는 마술(x1, x2 ROI 마스킹)
detection_frame[:x1, :] = 0 # y축 0~325 픽셀을 검은색(0)으로 칠함
detection_frame[x2:, :] = 0 # y축 635 픽셀 아래부터 끝까지 검은색(0)으로 칠함

이유: 하늘이나 도로 바깥은 신경 쓸 필요가 없는 영역이기 때문에 검은색으로 지워서 AI가 불필요한 곳에 집중하지 않게 만듭니다.

복원: AI가 박스를 친 후(processed_frame = res[0].plot()), 
processed_frame[:x1, :] = frame[:x1, :].copy()를 수행하여 아까 검은색으로 지웠던 배경을 원본이미지로 다시 덮어씌웁니다.
(결과적으로 도로 중앙에만 박스가 탐지되고 배경은 깨끗한 원본으로 보이게 만드는 테크닉입니다.)

[2] 다각형(사다리꼴 차선) 그리기(cv2.polylines)
cv2.polylines(processed_frame, [vertices1], isClosed=True, color=(0, 255, 0), thickness=2)

ㅇ vertices1, vertices2는 도로 위 4개의 점(X, Y) 좌표로 만든 사다리꼴 형태입니다.
ㅇ cv2.polylines()는 이 점들을 선으로 이어 도로 위에 "왼쪽 차선 다각형(초록색)", "오른쪽 차선 다각형(노란색)" 영역을 눈에 보이게 시각화해 줍니다.

[3] 좌우 차선 차량 수 세기(lane_threshold)
for box in boxes_det.xyxy: 
	x_center = (box[0] + box[2]) / 2 # Bounding Box의 X축 중앙값 
	if x_center < lane_threshold: 
		left_count += 1 
	else: 
		right_count += 1

ㅇ YOLO가 찾아낸 차량의 네모 박스 좌표(xmin, ymin, xmax, ymax)중 box[0] + box[2] / 2로 차량의 가로(X) 중앙 위치를 구합니다.
ㅇ 차선 경계선 기준(lane_threshold)보다 왼쪽에 있으면 left_count, 오른쪽에 있으면 right_count
ㅇ 차량 수가 기준치(heavy_threshold)를 넘으면 혼잡(heavy), 안 넘으면 원활(smooth)

-------------------------------------------------------------------------------

(1) 원본 동영상 정보(메타데이터) 가져오기
fps = cap.get(cv2.CAP_PROP_FPS) or 20.0 
width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH)) 
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
새 동영상 파일로 저장하려면 "초당 몇프레임으로 재생할건지(FPS)", 화면 가로/세로 크기는 얼마로 할건지"를 녹화기에게 알려줘야 합니다.

[1] fps = cap.get(cv2.CAP_PROP_FPS) or 20.0
cap.get(cv2.CAP_PROP_FPS): 원본 비디오 파일의 fps(예: 30.0, 60.0)를 읽어옵니다.
or 20.0: 일부 동영상 파일의 메타데이터 손상으로 fps 값을 못읽어올 때 쓰는 안전장치입니다.

(2) 동영상 압축 코덱 설정(fourcc)
fourcc = cv2.VideoWriter_fourcc(*'XVID')

FourCC(Four-Character Code)
동영상 파일은 그냥 그림을 묶어둔 게 아니라 용량을 줄이기 위해 압축되어 있습니다.
이때 "어떤 알고리즘(코덱)으로 비디오를 압축해서 저장할 것인가"를 정해주는 4자리 문자 코드입니다.
ㅇ *'XVID'의 언팩 기법: cv2.VideoWriter_fourcc 함수는 4개의 문자를 하나씩 전달받아야 합니다.
('X', 'V', 'I', 'D')
	ㅇ 파이썬에서는 문자열 앞에 *를 붙이면 글자를 하나씩 쪼개서 넘겨줍니다.
	cv2.VideoWriter_fourcc('X', 'V', 'I', 'D')와 같은 효과입니다.
ㅇ XVID 코덱: .avi 확장자 비디오 저장시 가장 보편적이고 범용적으로 쓰이는 코덱입니다.

-------------------------------------------------------------------------------

(1) res[0].boxes 객체 안에는 무엇이 있을까
YOLO 모델로 예측하면 결과물로 Results 객체(리스트 형태인 res[0])가 반환됩니다.
이 안의 boxes 객체 안에는 여러가지가 PyTorch Tensor(배열) 형태로 묶여 들어있습니다.
1. boxes.xyxy: 각 탐지 객체의 좌상단 우하단 좌표
2. boxes.xywh: 각 탐지 객체의 중심점 좌표와 가로/세로 길이
3. boxes.conf: 신뢰도 점수
4. boxes.cls: 탐지된 객체의 클래스id

box = [xmin, ymin, xmax, ymax]
```
![](images/Pasted%20image%2020260731121018.png)
![](images/Pasted%20image%2020260731121041.png)
![](images/Pasted%20image%2020260731121049.png)
# 179
```
# [세로 범위(Y), 가로 범위(X)]
array[Y_시작:Y_끝, X_시작:X_끝]
```
# 180
```
onnx_path = best_model.export(format='onnx')

(1) export()는 뭘 하는가
우리가 지금까지 다룬 가중치 파일(best.pt)은 파이토치 전용입니다.
하지만 실제 서비스 환경에서는 파이토치나 파이썬이 설치되지 않은 경우가 많습니다.
따라서 export()는 파이썬/파이토치가 없는 환경에서도 AI 모델이 돌아갈 수 있도록 규격화된 표준 포맷 파일로 내보내기하는 것입니다.

(2) ONNX (Open Neural Network Exchange)
ONNX는 AI 모델계의 PDF 파일입니다.
컴퓨터, 스마트폰, 어떤 기기에서든 뷰어만 있으면 바로 열어볼 수 있습니다.

ONNX 형태로 모델 변환을 해두면
1. 파이썬이 아닌 다른 언어 프로그램에서도 AI 모델을 불러와 예측할 수 있습니다.
2. 추론 속도가 파이토치 원본(.pt) 대비 1.5배~3배 빠릅니다.
```