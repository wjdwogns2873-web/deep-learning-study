# 201
```
results[0].masks는 이미지에서 검출된 마스크들의 정보가 모여있는 객체입니다. 탐지된 Pothole이 없다면 None이 될 수 있으므로 삼항연산자로 안전장치를 둡니다.

results[0].masks.data는 실제 마스크의 픽셀 좌표가 들어있는 그리드 데이터(Tensor)입니다.
len(results[0].masks.data)는 탐지된 마스크(Pothole)의 총 개수입니다.

masks = results[0].masks.data.cpu().numpy()
	ㅇ results[0].masks.data는 GPU 메모리에 올라와 있는 파이토치 텐서 형태입니다. 이것을 시각화하기 위해 CPU로 내리고, 넘파이 배열로 변환합니다.
	ㅇ 이 masks 배열의 형태는 [탐지된 개수, H, W] 구조를 가집니다. (예: [2, 640, 640])


binary_mask = (mask > 0).astype(np.uint8) * 255
	ㅇ YOLO의 Raw 마스크 데이터는 보통 0에서 1 사이의 실수나 bool 형태입니다.
	ㅇ mask > 0을 통해 객체가 있는 픽셀은 True, 배경은 False로 만듭니다.
	ㅇ .astype(np.uint8) * 255를 통해 True는 255(흰색), False는 0(검은색)을 가진 흑백 바이너리(이진) 이미지로 변환합니다.

--------------------------------------------------------------------------------

가공된 binary_mask 말고, 그냥 mask를.. axes[].imshow(mask, cmap='gray')
이렇게 넣어도 흑백사진이 똑같이 출력되는 이유?
	plt.imshow()의 오토 스케일링 (정규화) 효과이다.

plt.imshow() 함수는 가공되지 않은 raw 데이터를 입력받으면, 배열의 최솟값과 최댓값을 먼저 조사합니다.
가공되지 않은 mask의 최솟값은 0이고 최댓값은 1입니다.
imshow()는 최솟값(0)은 무조건 검은색, 최댓값(1)은 무조건 흰색으로 하자.라고 스스로 스케일링해버립니다.

그렇다면 굳이 binary_mask로 가공하는 이유는?
	matplotlib이 아닌 다른 라이브러리와의 호환성 및 확장성 때문이다.

OpenCV(cv2)와의 호환성: cv.imshow()나 cv.imwrite()는 오토 스케일링 기능이 없다.
픽셀 연산의 편의성: 데이터 타입을 8비트 정수형인 np.uint8로 맞춰두어야 추후 OpenCV를 이용해 마스크의 외곽선을 따거나(cv2.findContours), 이미지 합성 및 마스킹 연산(cv2.bitwise_and)같은 2차 가공을 에러 없이 할 수 있다.
```
![](images/Pasted%20image%2020260805232650.png)
# 202
```
[1] color_mask = cv2.cvtColor(binary_mask, cv2.COLOR_GRAY2BGR)
흑백 이미지 상태에서는 '초록색'선을 그릴 수 없다. 그래서 구조만 3채널 컬러(BGR) 형태로 방을 넓혀주는 작업입니다. (겉보기엔 여전히 흑백으로 보입니다.)

[2] cv2.findContours: 외곽선 찾기 (중요)

contours, _ = cv2.findContours(binary_mask, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)

역할: 흑백 이미지(binary_mask)에서 흰색(255) 덩어리의 가장자리 경계선 좌표들을 싹 다 찾아내는 함수입니다.

binary_mask: 반드시 0과 255로 이루어진 흑백 이미지여야만 작동합니다. 255를 곱해서 가공한 이유.
cv2.RETR_TREE: 외곽선을 찾을 때, 구멍 속에 또 구멍이 있는 등의 상하 관계(계층 구조)까지 싹 다 계보를 구해서 찾아달라는 옵션입니다.
cv2.CHAIN_APPROX_SIMPLE: 외곽선 좌표를 저장할 때 메모리를 아끼는 옵션입니다. 예를 들어 곧은 직선 경계선이 있다면, 그 곧은 직선을 지나는 모든 좌표를 저장하지 않고, 시작 좌표와 끝 좌표만 저장하겠다는 의미입니다.

반환값(contours, _): 결과로 2개를 주는데, 두 번째 값(계층구조)은 안쓰니까 무시하고 첫 번째 값인 contours(찾아낸 외곽선들의 리스트)만 받습니다.

[3] area = cv2.contourArea(contour)
외곽선 좌표(contour)를 집어넣으면 그 픽셀의 총 면적을 수학적으로 계산해 줍니다.

[4] cv2.drawContours(color_mask, [contour], -1, (0, 255, 0), 3)
역할: 방금 만든 color_mask 이미지 위에, 우리가 찾은 [contour] 선을 그려 넣습니다.
(0, 255, 0): Green. 초록색 선
3: 선의 두께
```
![](images/Pasted%20image%2020260806011503.png)
# 203
```
!ffmpeg -y -loglevel panic -i /content/runs/segment/predict/sample_video.avi /content/processed_sample_video.mp4

Video("/content/processed_sample_video.mp4", embed=True, width=960)

.avi를 .mp4로 변환하는 이유
구글 크롬이나 사파리같은 웹 브라우저는 보안과 압축 효율성 문제로 인해 .avi 포맷의 영상을 지원하지 않습니다.

YOLO 모델은 동영상을 추론할 때 내부적으로 OpenCV 엔진을 사용해 결과물을 저장하는데, 이때 기본적으로 .avi로 저장되는 경우가 많습니다.


ffmpeg: 전세계에서 가장 많이 쓰이는 컴퓨터용 동영상 오디오 편집/인코딩 프로그램
-y: 만약 똑같은 이름의 변환된 파일(processed_sample_video.mp4)이 이미 존재한다면, 묻지 말고 덮어쓰기하라.
-loglevel panic: 변환 과정에서 나오는 수많은 로그를 화면에 띄우지 말고, 치명적인(panic) 에러가 아니면 조용히 처리하라.
-i: 입력 파일의 경로를 지정합니다.

embed=True: 동영상 데이터를 노트북 파일 자체에 내장(embedding)시켜서, 나중에 노트북을 열었을때도 끊김없이 볼 수 있게끔 하라
```
# 204
```
ret (True/False): 다음 사진을 성공적으로 가져왔는지 알려주는 신호등입니다.
frame: 가져온 한장의 이미지 데이터(프레임)입니다.

영상 위에 단순히 매 프레임당 계산된 피해량(%)을 쓰면 1.2% -> 5.8% -> 0.3% 이런식으로 파르르 떨리면서 가독성이 떨어집니다. 이를 막기 위해 deque(maxlen=10)을 썼습니다.

deque는 맥스 길이(여기서는 10)를 설정하면 맥스 길이만큼 데이터가 저장됩니다.
11번째 데이터가 들어오면 가장 처음에 들어왔던 옛날 데이터 1개를 뱉어내고 항상 10개의 데이터만 유지합니다.

sum(damage_deque) / len(damage_deque): 최신 10개 프레임의 피해량을 평균 냅니다.
이를 통해 숫자가 요동치지 않고 부드럽게 변화합니다. 이것을 이동 평균(Moving Average) 필터라고 합니다.


cv2.line(processed_frame, (text_position[0], text_position[1] - 10), (text_position[0] + 350, text_position[1] - 10), background_color, 40)

그냥 이미지 위에 흰색 글씨를 쓰면 가독성이 안좋으므로 글씨 뒤에 빨간색 배경을 만들어 가독성을 높입니다.
사각형을 그리는 cv2.rectangle 대신, 두께 40짜리 아주 두꺼운 직선(cv2.line)을 가로로 그어서 꽉 찬 사각형 박스처럼 보이게 합니다.
```
![](images/Pasted%20image%2020260806020307.png)
![](images/Pasted%20image%2020260806020330.png)
# 205
```
img = cv2.imread(image_path)로 이미지를 읽을 때
img.shape는 (높이, 너비, 채널 수)를 반환함.

img = Image.open(image_path)로 이미지를 읽을 때
img.size는 (너비, 높이)를 반환함
```
![](images/Pasted%20image%2020260806150358.png)
# 206
```
file = '/content/dataset/car_part_dataset/images/test/847.jpg'

dirpath = os.path.dirname(file)
# dirpath = '/content/dataset/car_part_dataset/images/test'

folder_name = os.path.basename(dirpath)
# folder_name = 'test'

[1] os.path.dirname(path)
역할: 전체 경로에서 가장 마지막 요소만 제외한 나머지 경로를 반환합니다.

[2] os.path.basename(path)
역할: 전체 경로에서 가장 마지막 요소만 딱 잘라서 반환합니다.
```
# 207
```
seg[:, 0], seg[:, 1] 구조와 seg의 shape

YOLO는 물체 테두리를 찾으면, 그 테두리를 이루는 수많은 점들의 좌표 목록을 줍니다.
따라서 seg는 [점의 개수, 2] 모양(shape)을 가진 2차원 넘파이 배열입니다.

점의 개수가 4개라고 할때, 
0번 열이 있고 1번 열이 있습니다. 0번 열은 X좌표이고, 1번 열은 Y좌표입니다.
shape: [4, 2]

[[0.25, 0.40], # 0번 좌표
 [0.75, 0.45], # 1번 좌표
 [0.70, 0.85], # 2번 좌표
 [0.30, 0.80]] # 3번 좌표

각 좌표는 0~1 값으로 정규화 되어있습니다. 이걸 원래 사이즈로 복원해주기 위해서 X좌표에는 w(너비), Y좌표에는 h(높이)를 곱합니다.
```
![](images/Pasted%20image%2020260806205725.png)
# 208
```
cv2.addWeighted()
이 함수는 두 영상을 "원하는 비율만큼 섞어서 반투명하게 겹쳐주는" 역할을 합니다. 포토샵의 레이어 투명도 조절과 같습니다.
	결과 이미지 = (이미지1 * a) + (이미지2 * b) + c

cv2.addWeighted(overlay, alpha, img, 1 - alpha, 0, img)

1. overlay: 색칠된 도형이 그려진 투명용지 레이어
2. alpha(0.4): overlay를 얼마나 반영할지 결정.(40%의 선명도)
3. img: 글씨와 테두리 선이 그려진 밑바탕 이미지 레이어
4. 1 - alpha(0.6): img를 얼마나 반영할지 결정.(60%의 선명도)
5. 0: 감마 값입니다. 합성 후 이미지 전체 밝기를 추가로 올리거나 내릴 때 쓰는데, 보통 0을 넣습니다.
6. img: 최종 합성된 결과물을 저장할 변수입니다. (기존 변수에 덮어쓰기)

```
![](images/Pasted%20image%2020260806210551.png)
# 209
```
이미지 세그멘테이션(Image Segmentation)

이미지에서 픽셀 단위로 관심 객체를 추출하는 방법
이미지를 분할해 이미지의 각 픽셀에 레이블을 부여하는 것이다.
이러한 세그멘테이션은 여러가지 종류로 나뉜다.

(1) 시멘틱 세그멘테이션 (Sementic Segmentation)
이미지에서 픽셀을 사람, 자동차, 비행기 등의 물리적 단위로 분류하는 방법이다.

(2) 인스턴스 세그멘테이션 (Instance Segmentation)
시멘틱은 사진에서 '사람'이라는 객체 자체를 추출한다. 결국 다른 사람이더라도 같은 라벨로 표현된다.
하지만 인스턴스 세그멘테이션은 사람 개개인별로 다른 라벨을 가지게 한다.
```
![](images/Pasted%20image%2020260807113902.png)
# 210
```
시멘틱 세그멘테이션 활용
시멘틱을 해결하기 위해 여러가지 방법이 있는데, 그중 DeepLab이라 불리는 알고리즘이 있다.

DeepLab은 시멘틱을 해결하기 위한 방법으로 1, 2, 3, 3+ 네가지 버전이 있다.
DeepLab은 atrous convolution을 활용해 시멘틱을 해결한다.

[1] atrous convolution
atrous convolution은 기존 convolution과 다르게 필터 내부에 빈 공간을 둔 채로 작동한다.
r(rate)이라는 파라미터를 통해 얼마나 빈 공간을 둘지 설정하고 빈 공간을 제외하고 기존 convolution과 같이 학습을 진행한다.

장점
	ㅇ 기존 convolution과 동일한 양의 파라미터와 계산량을 유지하면서 field of view(한 픽셀이 볼 수 있는 영역)를 크게 가져갈 수 있다.
	ㅇ 즉, 파라미터 수를 늘리지 않으면서도 receptive field를 키울 수 있다.
			receptive field란? 필터가 한번 보는 영역으로 사진의 feature를 추출하기 위해선 
			receptive field가 높을수록 좋다.

[2] spatial pyramid pooling
시멘틱의 성능을 높이기 위한 방법 중 하나로, 이를 사용한 SPPNet은 R-CNN의 단점을 극복하고자 나왔다.

ㅇ SPP는 Conv Layer를 거친 Feature Map을 인풋으로 받아 4x4, 2x2, 1x1로 미리 정해진 영역으로 나눈다.
ㅇ 각각을 하나의 피라미드라고 부른다. 아래 사진에서는 3개의 피라미드를 설정한 것이다.
ㅇ 피라미드에서 네모칸 한 칸을 bin이라고 부른다.
ㅇ 각 bin에 알맞은 크기의 Window와 Stride를 통해 Feature Map을 순회하며 MaxPooling을 수행해 그 값을 이어붙인다.
```
![](images/Pasted%20image%2020260807114319.png)
![](images/Pasted%20image%2020260807115034.png)
# 211
```
U-Net
ㅇ 인코더(Encoder - 수축 경로): 이미지를 줄여가며 무엇(What)이 있는지 고차원 특징을 추출합니다.
ㅇ 디코더(Decoder - 확장 경로): 줄어든 특징 맵을 다시 원래 이미지 크기로 복원하면서 어디(Where)에 위치하는지 픽셀 단위로 위치를 되찾습니다.
ㅇ 스킵 연결: 인코더에서 축소되기 전의 선명한 위치 정보를 디코더로 바로 넘겨주어, 디테일한 경계선을 잃지 않게 돕습니다.

손실 함수
세그멘테이션 손실 함수로 자주 쓰이는 Dice Loss는 두 영역의 겹침 정도를 나타냅니다.
```
![](images/Pasted%20image%2020260807201607.png)
# 212
```
mask = cv2.imread(mask_path, 0)
여기서 0은 흑백(Grayscale) 모드로 읽어오라는 의미입니다.

0 (또는 cv2.IMREAD_GRAYSCALE)
	흑백 모드로 이미지 읽음
	이미지를 채널이 1개인 2차원 배열로 불러옵니다.

1 (또는 cv2.IMREAD_COLOR)
	기본값. (아무것도 안 적으면 1로 동작)
	이미지를 BGR 컬러 모드로 읽습니다.
	가로 x 세로 x 3채널(B, G, R)의 3차원 배열로 불러옵니다.
```
# 213
```
cv2.imwrite()는 이미지 배열 데이터를 지정한 경로와 파일 형식으로 저장하는 함수입니다.
문법은 cv2.imwrite(filename, image, params)

[파이썬 코드]
img = cv2.imread('input.jpg')

success = cv2.imwrite('output.jpg', img)
```
# 214
```
np.random.randint(0, 256, (256, 256, 3), dtype=np.uint8)

0부터 255까지의 랜덤한 숫자로 채워진 (256, 256, 3) 크기의 컬러 이미지(3채널)를 만들어라.

np.random.choice([0, 255], (256, 256), p=[0.7, 0.3])

0과 255 중에서만 숫자를 고르되, 0은 0.7의 확률로, 255는 0.3의 확률로 골라서 (256, 256) 크기의 흑백 마스크를 만들어라.
```
![](images/Pasted%20image%2020260807232249.png)
# 215
```
cv2.imread(image_path)
	이미지를 읽어와 넘파이 배열 형태로 메모리에 올립니다.

transforms.ToPILImage()
	넘파이 배열을 PyTorch 전처리 함수들이 인식할 수 있는 PIL 이미지 형태로 바꿉니다.

transforms.ToTensor()
	PIL 이미지를 최종적으로 모델 입력에 필요한 PyTorch 텐서로 변환합니다.
	이 과정에서 0~255 픽셀 값이 0.0~1.0 실수(float)로 자동 정규화(Scaling)됩니다.
```
# 216
```
왜 마스크에는 NEAREST 보간법을 써야 할까?

최근접 이웃 보간법(nearest neighbor): 크기를 조절할 때 주변에 있는 가장 가까운 픽셀 값을 그대로 가져옵니다. 0과 1 사이에 새로운 값을 만들지 않으므로 라벨이 깨지지 않습니다.
```
![](images/Pasted%20image%2020260808020919.png)
# 217
```
활성화 함수를 쓰는 이유
합성곱(Conv2d) 연산은 아무리 많이 쌓아도 결국 직선이다.
하지만 우리가 판별해야 하는 이미지 데이터는 매우 복잡한 곡선(비선형 데이터) 형태를 띤다.
활성화함수는 데이터에 비선형성을 부여하여 모델이 복잡한 패턴을 배울 수 있게 한다.

많은 활성화 함수 중에 왜 ReLU를 쓰는 이유?
0보다 작으면 날리고, 0보다 큰 값은 그대로 통과시키는 아주 단순한 구조이다.
sigmoid에 비해 계산이 압도적으로 빠르다.
```
# 218
```
합성곱 -> 활성화 -> 맥스풀링 순서인 이유
이것은 인간이 눈으로 사물을 인식하는 과정을 그대로 구현한 것이다.

1. 합성곱: 특징 추출 (눈으로 훑기)
   이미지 위로 필터를 훑으면서 "여기는 세로선, 여기는 눈"같은 시각적 특징을 찾아냅니다.

2. 활성화 함수: 의미 있는 정보 필터링 (뇌로 판단)
   합성곱을 거쳐 나온 수많은 숫자 중, 의미 없는 노이즈(음수)는 0으로 날려버리고, 유의미한 특징만 활성화시킵니다.

3. 맥스풀링: 핵심 요약 & 크기 축소 (기억 저장)
   이미지 크기를 줄이면서 그 구역에서 가장 강렬한 특징만 뽑아서 요약합니다.
```
# 219
```
세그멘테이션에서 인코더로 압축했다가 디코더로 복원하는 과정에서 원본 이미지의 미세한 위치 정보 및 디테일은 심각하게 훼손된다.
합성곱 -> 맥스풀링을 거치면서 "여기에 물체가 있다"라는 대략적인 특징은 남지만, 복원하는 과정에서 원본은 훼손된다.
그래서 디코더가 아무리 ConvTranspose2d로 크기를 뻥튀기해도 결과물이 흐릿하게 나온다.

UNet은 스킵 커넥션(Skip Connection)을 통해 이 문제를 해결했다.
1. 인코더가 압축하기 전의 이미지를 복사해 둡니다.
2. 디코더가 압축된 정보를 겨우 뻥튀기해 놓으면, 아까 복사해 둔 원본을 옆에 그대로 이어붙입니다.(concatenate)
   
[뻥튀기된 데이터] + [복사본] 합체


-------------------------------------------------------------------------------
인코더가 하는 일: 입력받은 사진을 반으로 줄이고, 또 반으로 줄이면서(맥스풀링) 핵심 특징만 압축합니다.
복사의 타이밍: 이 압축(풀링) 단추를 누르기 직전에, 특징이 살아있는 그 상태를 복사합니다.
디코더로 전달: 디코더가 나중에 작아진 이미지를 다시 키웠을 때, 아까 복사해 두었던 카피본을 옆에 붙입니다.
```
# 220
```
1. preds > threshold (이진화 작업)
ㅇ 모델의 디코더 끝에서 나온 출력(preds)은 sigmoid를 거쳐 0.0~1.0 확률 값으로 되어 있습니다. 이를 기준점(threshold)을 넘으면 True(객체), 넘지 않으면 False(배경) 형태의 불 텐서로 바꿉니다.

2. intersection과 union
ㅇ (preds_bool[i] & masks_book[i]).sum().item(): 모델의 예측과 실제 정답이 둘 다 true인 영역의 픽셀 개수를 셉니다.
ㅇ (preds_bool[i] | masks_bool[i]).sum().item(): 합집합 영역의 픽실 게수를 셉니다.



outputs의 shape: (8, 256, 256)
masks의 shape: (8, 256, 256)
```
![](images/Pasted%20image%2020260808124651.png)
