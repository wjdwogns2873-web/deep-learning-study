# 121
```
객체 탐지 모델 학습 후 생성되는 파일들..

(1) confusion_matrix.png (혼동 행렬)
모델이 어떤것을 헷갈려 하는지를 볼 수 있는 그래프이다.
이것을 보고 분석해서 예를 들어 "캔과 페트병을 헷갈려한다면 캔과 페트병 데이터를 더 수집해야 겠구나" 하고 개선 방향을 잡을 수 있다.

(2) val_batch0_labels.jpg vs val_batch0_pred.jpg
_labels는 Validation의 진짜 정답 박스이고, _pred는 모델이 예측해서 친 네모박스이다.
숫자로 된 점수(mAP)만 보면 눈에 안 보이니까 이 두 이미지를 양옆에 띄워두고 시각적으로 비교해 보는것이다.

(3) F1_curve.png, P_curve.png, R_curve.png
신뢰도 임계값(모델이 몇% 확신할 때 박스를 화면에 띄울 것인가)에 따라 Precision과 Recall이 어떻게 변하는지 보여주는 그래프

```
![](images/Pasted%20image%2020260704211401.png)
# 122
```
conf의 값을 1.0 근처에 도달하게 하려면 학습을 많이 하는 게 정답인가? 절대 아니다.
에폭만 100, 1000으로 늘린다고 절대 그렇지 않다.
에폭 수보다 '데이터의 양과 질'이 훨씬 중요하다.

지금의 데이터는.. 네모박스가 그려져 있지 않은 이미지가 너무 많다.
노이즈 있는 1000장의 데이터보다 꼼꼼하게 네모박스 치고, 노이즈 없는 100장의 데이터가 훨씬 좋다.
데이터의 양과 질 둘 다 중요하지만 질을 먼저 챙기고 양을 챙기자.
```
# 123
```
[1] 이미지 분류 (PIL -> PyTorch Tensor 방식)

[파이썬 코드]
contents = await file.read()
pil_image = Image.open(io.BytesIO(contents)).convert('RGB')
image = np.array(pil_image)

파일의 바이트 데이터를 io.BytesIO로 감싸서 메모리상의 가상 파일로 만든 뒤, PIL 라이브러리의 Image.open()으로 읽는다.

albumentations 가공 단계로 넘어가기 위해 데이터를 RGB 채널의 NumPy 배열로 바꾸는 전통적인 파이토치 방식이다.


[2] 객체 탐지 (OpenCV 바이트 디코딩 방식)

[파이썬 코드]
contents = await file.read()
nparr = np.frombuffer(contents, np.uint8)
img = cv2.imdecode(nparr, cv2.IMREAD_COLOR)

파일의 바이트 데이터를 컴퓨터가 바로 읽을 수 있는 부호 없는 8비트 정수 배열로 변환한 뒤, OpenCV의 cv2.imdecode()를 이용해 메모리상에서 바로 이미지 객체로 압축을 풀었다.


[3] 이미지 분류 때 쓰던 방식으로 해도 될까?

[파이썬 코드]
contents = await file.read()
pil_image = Image.open(io.BytesIO(contents)).convert('RGB')
image_rgb = np.array(pil_image)
# YOLO는 BGR 순서를 좋아하니까 BGR로 변환해준다.
img = cv2.cvtColor(image_rgb, cv2.COLOR_RGB2BGR)

result = model(img, conf=0.01)

하지만 OpenCV의 cv2.imdecode() 방식이 메모리를 덜 쓰고 처리속도가 더 빨라서 웹서버(FastAPI)나 실시간 영상 Serving을 할 때는 cv2.imdecode() 방식을 더 선호한다.
```
# 124
```
aihubshell 사용 방법

목록 조회
aihubshell -mode l | grep 농산물
aihubshell -mode d - datasetkey 149 -filekey 396903 -aihubapikey "xxx"
```
# 125
```
(1) 파이썬에서 디렉토리 생성

import os

# 생성할 디렉토리 경로 지정
dir_path = "path/to/new_folder"

# 디렉토리 생성
os.makedirs(dir_path, exist_ok=True)


(2) 파이썬에서 파일 존재 여부 확인하기

import os

file_path = "example.txt"

# 특정 파일이 존재하는지 확인
if os.path.isfile(file_path):
	print("파일이 존재합니다.")
else:
	print("파일이 존재하지 않습니다.")

# 파일/폴더 상관없이 존재 여부만 확인
if os.path.exists(file_path):
	print("대상 경로가 존재합니다.")

(3) 파이썬에서 파일 복사하기

import shutil

# shutil.copy("원본 경로", "대상 경로")
# 대상 경로에 폴더명을 쓰면 해당 폴더 안에 같은 이름으로 복사된다.
shutil.copy("source.txt", "destination_folder/")
shutil.copy("source.txt", "destination_folder/copied_file.txt")

(4) 파이썬에서 폴더 하위 파일 및 폴더 삭제하기

import shutil

# 삭제할 폴더 경로
dir_path = "path/to/delete_folder"

# 폴더와 내부 콘텐츠 일괄 삭제
shutil.rmtree(dir_path)
```

# 126
```
파이썬에서 확장자 제외한 부분만 가져오기

(1) pathlib 라이브러리 사용 (추천)
from pathlib import Path

file_path = "/User/documents/project/image123.jpg"

pure_name = Path(file_path).stem

print(pure_name) # 출력: image123


(2) os.path.splittext 사용 (기존 방식)

import os

file_path = "/User/documents/project/image123.jpg"

file_name_only, ext = os.path.splitext(file_path)

print(file_name_only) # 출력: /User/documents/project/image123
print(os.path.basename(file_name_only)) # 출력: image123
```
# 127
```
파이썬에서 파일 생성하기(write)

with open("test.txt", "w", encoding="utf-8") as f:
	f.write("첫번째 줄\n")
	f.write("두번째 줄\n")
```
# 128
```
파이썬에서 json 파일 읽어오기

import json
test_basename = "apple_fuji_L_1-1_1TOP"
with open(test_basename + ".json") as f:
	data = json.load(f)

# 키값 출력
print(data.keys())
# 밸류값 출력
print(data.values())

# 키 - 밸류 출력
print(json.jumps(data, indent=4, ensure_ascii=False))
```
# 129
```
파이썬에서 리스트의 요소를 무작위로 섞기

import random

# 시드를 지정해주면 항상 같은 순서로 섞을 수 있다.
random.seed(42)

my_list = [1, 2, 3, 4, 5]
random.shuffle(my_list)
```
# 130
```
파이썬에서 리스트 슬라이싱을 활용하여 8:2 비율로 분할하기

data = list(range(100))

split_idx = int(len(data) * 0.8)

train_data = data[split_idx:]
val_test_data = data[:split_idx]
```
# 131
```
객체탐지에서는 WeightRandomSampler를 쓰기가 매우 어렵다. 한 장의 이미지 안에 여러 개의 클래스가 들어있을 수 있기 때문이다. 그래서 객체탐지에서는 Focal Loss로 불균형을 해소한다.

이미지 분류에서는 Sampler로 데이터 양을 조절했다면, 객체탐지에서는 "다수 클래스에는 벌금을 적게 매기고, 소수 클래스에는 벌금을 세게 때리는" Focal Loss를 쓴다.

YOLOv8 내부에는 데이터 불균형을 완화하기 위해 Focal Loss 연산이 탑재되어 있다.
하이퍼파라미터 튜닝시 fl_gamma 값을 조절해 이 벌금의 강도를 제어할 수 있다.

```
# 132
```
파이썬에서 파일 읽기

(1) read(): 파일 전체를 하나의 문자열로 읽기
with open("example.txt, "r", encoding="utf-8") as f:
	content = f.read()
	print(content)
	
(2) readlines(): 모든 줄을 리스트로 읽기
with open("example.txt", "r", encoding="utf-8") as f:
	lines = f.readlines()
	for line in lines:
		print(line)
```
# 133
```
plt를 사용하여 수직 및 수평 막대 그래프 생성하기

import matplotlib.pyplot as plt
import numpy as np

categories = ["A", "B", "C", "D", "E"]
values = [15, 32, 2, 2, 1]

plt.bar(categories, values, color="skyblue")

plt.title("Bar Title")
plt.xlabel("Category")
plt.ylabel("Value")

plt.show()
```
# 134
```
data.yaml에 있는 문자열 가져오기

import yaml
import matplotlib.pyplot as plt

with open("data.yaml", "r", encoding="utf-8") as f:
	yaml_data = yaml.safe_load(f)

categories = yaml_data["names"] # ['Apple', 'Banana'] 추출
print(categories) # ['Apple', 'Banana']
```
# 135
```

```
# 136
```

```
# 137
```

```
# 138
```

```
# 139
```

```
# 140
```

```